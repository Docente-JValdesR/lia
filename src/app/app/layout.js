export const metadata = {
  title: "Practicar",
  description:
    "Configura una actividad de lectura por nivel, unidad y habilidades, mide tu velocidad lectora y descubre qué practicar después con L+IA.",
  alternates: { canonical: "/app" },
};

export default function AppLayout({ children }) {
  return children;
}
